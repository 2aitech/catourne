{
  "name": "@casting/ml",
  "private": true,
  "scripts": {
    "dev": "python3 -m uvicorn src.server:app --reload --host 0.0.0.0 --port ${ML_PORT:-3010}",
    "start": "python3 -m uvicorn src.server:app --host 0.0.0.0 --port ${ML_PORT:-3010}",
    "build": "python3 -m compileall src",
    "train": "python3 src/training/train.py"
  }
}
